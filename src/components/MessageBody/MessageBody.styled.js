import styled from 'styled-components';

export const Wrapper = styled.div`
  .title {
    font-weight: 500;
    font-size: 24px;
    line-height: 1.33;
  }

  .main-word {
    font-style: italic;
    font-weight: 400;
    background: #fbe9ba;
    border-radius: 8px;
    padding: 0 4px;
  }
`;
